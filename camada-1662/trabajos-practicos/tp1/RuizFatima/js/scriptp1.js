var number1 = prompt ("Ingrese el primer numero");
var number2 = prompt ("Ingrese el segundo numero");
var parsedNumber1 =  Number(number1);
var parsedNumber2 =  Number(number2);
console.log (parsedNumber1 + parsedNumber2)
alert ("La suma de los dos numeros es " + (parsedNumber1 + parsedNumber2));
alert ("La resta de los dos numeros es " + (parsedNumber1 - parsedNumber2));
alert ("La multiplicación de los dos numeros es " + (parsedNumber1 * parsedNumber2));
alert ("La división de los dos numeros es " + (parsedNumber1 / parsedNumber2));